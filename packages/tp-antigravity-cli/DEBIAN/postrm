#!/bin/bash
set -e

if [ "$1" = "remove" ] || [ "$1" = "purge" ]; then
    if ! dpkg-query -W -f='${Status}' tp-intellij-idea 2>/dev/null | grep -q "ok installed"; then
        rm -f /etc/profile.d/tp-intellij-agy-interop.sh 2>/dev/null || true
    fi
fi

exit 0
